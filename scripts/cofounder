#!/usr/bin/env bash
# SiftWorks launcher — Co-founder seat.
# Uses claude --dangerously-skip-permissions (claudeyolo equivalent) with Opus 4.8 max effort.
exec env SIFTWORKS_SEAT=co-founder claude --dangerously-skip-permissions "$@"
