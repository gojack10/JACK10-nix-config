#!/bin/sh
# music — launch nvim into the music plugin on a given folder (default: $PWD)
set -e

dir="${1:-$PWD}"
abs="$(cd "$dir" 2>/dev/null && pwd)" || { echo "music: no such directory: $dir" >&2; exit 1; }

export MUSIC_CWD="$abs"
exec nvim -c "lua require('music').start(vim.env.MUSIC_CWD)"
